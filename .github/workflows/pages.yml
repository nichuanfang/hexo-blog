name: Pages

on:
  push:
    branches:
      - main # default branch

jobs:
  pages:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v2
        with: 
          path: hexo-blog
      - name: Use Node.js 14.x
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Cache NPM dependencies
        uses: actions/cache@v2
        with:
          path: node_modules
          key: ${{ runner.OS }}-npm-cache
          restore-keys: |
            ${{ runner.OS }}-npm-cache

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: checkout出nginx-config
        uses: actions/checkout@v2
        with:
          repository: nichuanfang/nginx-config
          token: ${{ secrets.GH_TOKEN }}
          ref: main
          path: nginx-config

      - name: 更新webroot
        run: |
          cd $GITHUB_WORKSPACE/nginx-config/webroot
          rm -rf *
          cp -r $GITHUB_WORKSPACE/hexo-blog/public/* .

      - name: 更新nginx-config项目 触发私服部署
        uses: Smart-Transportation/push@v1.3
        with:
          github_token: ${{ secrets.GH_TOKEN }}
          branch: 'main'
          author_email: 'github-actions[bot]@users.noreply.github.com'
          author_name: 'github-actions[bot]'
          message: '🐳 chore: 更新博客'
          directory: /home/runner/work/hexo-blog/hexo-blog/nginx-config
          repository: nichuanfang/nginx-config

      - name: 部署到github
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.GH_TOKEN }}
          external_repository: nichuanfang/nichuanfang.github.io
          publish_branch: gh-pages # default: gh-pages
          publish_dir: ./public
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
